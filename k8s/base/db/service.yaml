# ── DB Service Service ────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: db-service
  labels:
    app: db-service
    app.kubernetes.io/part-of: archaics
spec:
  selector:
    app: db-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
