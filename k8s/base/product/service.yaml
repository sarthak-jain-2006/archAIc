# ── Product Service Service ───────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
    app.kubernetes.io/part-of: archaics
spec:
  selector:
    app: product-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
